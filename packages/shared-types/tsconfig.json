{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // Test files only — the package itself stays dependency-free (ARCHITECTURE.md).
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
